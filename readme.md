Creating controller with model:
    php artisan make:controller MerchantTokenController --model=MerchantToken
    
project setup:
clone the project
download composer
install dependencis -- composer install
then run server

how to run server:
    php artisan serve